# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js / build
.next/
out/
build/
dist/
next-env.d.ts

# Environment / secrets (Turso token etc. — config via Vercel env vars)
.env
.env.local
.env.*.local

# Vercel
.vercel

# PWA generated (next-pwa / Serwist) — o serwist reescreve tudo isso a cada build
public/sw.js
public/sw.js.map
public/swe-worker-*.js
public/swe-worker-*.js.map
public/workbox-*.js
public/worker-*.js
public/fallback-*.js

# v1 app — separate git repo with its own history; not nested here (migrate deliberately if needed)
/BatePerna/

# Claude Code local tooling (installed skills + machine-specific settings — regenerable)
.claude/

# Agent-skill tooling auto-instalado por integrações (ex.: Turso via Vercel Marketplace) — regenerável
.agents/
skills-lock.json

# Logs / OS / editor
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/
*.tsbuildinfo
.env*

# Scratch do superpowers (mockups de brainstorm, sessoes). Specs e planos moram em docs/.
.superpowers/
